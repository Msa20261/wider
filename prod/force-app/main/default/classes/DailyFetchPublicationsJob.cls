public class DailyFetchPublicationsJob {

    // this invocable method is not used anymore
    // but still kept in flow for potential future use
    @InvocableMethod(label='Simap Fetch')
    public static void fetchSimap() {
        System.debug('Invocable method from flow is not implemented anymore for this project');
        return;
    }

}